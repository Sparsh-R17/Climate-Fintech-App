import 'package:flutter/material.dart';

class HostDonationScreen extends StatelessWidget {
  const HostDonationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Here user can host donations'),
    );
  }
}
